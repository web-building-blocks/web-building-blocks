import PageTheme from "../components/layout/pageTheme";

# Static Layout Page

**Contributed by Chaoyang Li**

Static Layout has fixed content.It is the easiest layout of website to create.Static websites are build of HTML, CSS, and JavaScript.

Static Layout are quick and easy to create. It is cheap to host. Static Layout are secure.

The code below creates a static page about the University of Sydney.
**HTML** code creates the structure, of the page, it is the standard markup language for creating web pages.
**CSS** code sets the layout of your web page with beautiful colors, fonts, and much more.
**JS** code can make your page interactive.

<PageTheme pageName="staticLayout" />
